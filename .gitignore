# .gitignore - template focado em projetos Docker
# Substitui o template anterior (Python) por um conjunto de regras
# voltadas a ignorar arquivos e artefatos locais/temporários criados
# quando se desenvolve e testa com Docker / Docker Compose.

# Arquivos de ambiente locais (contêm segredos/configs locais)
.env
.env.*.local
.env.local
.envrc

# Overrides / arquivos de configuração locais do docker-compose
docker-compose.override.yml
docker-compose.*.local.yml
docker-compose.local.yml

# Arquivos de imagem exportada / pacotes gerados pelo Docker
*.tar
*.tar.gz
*.tgz

# Diretórios/arquivos gerados por ferramentas e caches de build
.docker/
docker-build/
docker_cache/
build/
tmp/
temp/
run/
logs/
*.log

# Docker Desktop / docker-machine / contextos locais
docker-desktop/
.docker-machine/

# Kubernetes / Helm locais (quando usado junto com Docker)
.kube/
kubeconfig
helmfile.d/

# Arquivos de orquestração / estado local
*.pid
*.seed
*.swp

# IDEs e editores (ajuste conforme sua preferência)
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Sistema operacional
.DS_Store
Thumbs.db

# Arquivos de projeto que podem conter credenciais (adapte conforme necessário)
credentials/
secrets/
*.key
*.pem

# Logs de containers locais
docker-logs/
container-logs/

# Notebooks e caches (se aplicável)
.ipynb_checkpoints
__pycache__/

# Arquivos gerados por CI locais / runners
runner/
ci_artifacts/